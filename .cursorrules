# Legacy Cursor Rules

Cursor loads optional project rules from `.cursor/rules/` on disk. That directory is gitignored here so each clone can keep local rules without putting them in the remote tree.

In the repository, treat `AGENTS.md` and `PRD.md` as the authoritative agent instructions. This legacy file exists only to redirect older Cursor setups and should not contain independent project constraints.
